// src/theme/borderRadius.ts
/**
 * Border‑radius tokens (Tailwind config) – values are in logical pixels.
 */
export const borderRadius = {
  DEFAULT: 2, // 0.125rem → 2 px
  lg: 4,      // 0.25rem → 4 px
  xl: 8,      // 0.5rem → 8 px
  full: 12,   // 0.75rem → 12 px
};
